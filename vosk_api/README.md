Installation instructions:

Navigate to this folder in your CLI (cd vosk_api), then execute following commmands

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python server.py